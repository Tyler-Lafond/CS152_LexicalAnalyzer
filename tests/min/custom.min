function potato;
beginparams
    n : integer;
    l : integer;
    k : integer;
    none : array [1] of integer;
endparams
beginlocals
endlocals
beginbody
    n := 10 + 3 * 3 * 11 * n;
    return n;
endbody

function main;
beginparams
endparams
beginlocals
    fib_n : integer;
    stuffs : array [20] of integer;
endlocals
beginbody
    n := 3 * - 5 + - 2;
    write n;
    n := - 2 + 3 * - 5;
    write n;
    n := 10 + 3 * 4;
    write n, stuffs[1];
    if (n <= 1) then fib_n := 1; else fib_n := potato(n); endif;
    write n, fib_n;
endbody
